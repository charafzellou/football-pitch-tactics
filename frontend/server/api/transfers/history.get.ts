// This is a placeholder for transfer history.
// In a real application, you would create a `transfers` table
// and log each transfer.

export default defineEventHandler(async () => {
  return []
})
